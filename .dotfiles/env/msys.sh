#!/bin/bash
# uncomment to disable mangling windows paths
# some tools may fail to work without path mangling in msys, such as node
# recommended to disable explicitly instead by preprending ENV VAR to commands
# export MSYS_NO_PATHCONV=1 # prevent msys from mangling all / paths to windows equivalents
